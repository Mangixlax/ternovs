import {
  Swiper,
  Pagination,
  Navigation,
  Lazy,
  Controller,
  Thumbs,
  Zoom,
} from 'swiper/core'

import '~/scss/_swiper.scss'

Swiper.use([Pagination, Navigation, Lazy, Controller, Thumbs, Zoom])
